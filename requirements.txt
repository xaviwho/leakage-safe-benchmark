# Pinned dependencies for the leakage-safe benchmark (Python 3.14, CPU-only).
# Versions reflect the environment used to produce the committed results/ and figures.
# Install with:  pip install -r requirements.txt

# Core scientific computing
numpy==2.4.3
scipy==1.17.1
pandas==2.3.3

# Machine learning
torch==2.11.0            # CPU build; no GPU required
scikit-learn==1.8.0

# Single-cell I/O and state construction
scanpy==1.12.1
anndata==0.12.11
h5py==3.16.0

# Figures
matplotlib==3.10.8

# Utilities
pyyaml==6.0.3            # config loading (src/utils/config_loader.py)
tqdm==4.67.3             # training progress (src/models/predictors/trainer.py)

# Tests (optional; not pinned)
# pytest
